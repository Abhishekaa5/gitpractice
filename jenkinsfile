pipeline {
    agent any

    stages {
        stage("version"){
            steps {
                sh ("python3 --version")
            }
        }
        stage('pythonrun'){
            steps{
            sh ("python3 abhi.py")
            }
        }
    }
}