pipeline {
    agent any

    stages {
        stage("version"){
            steps {
                sh ("python3 --version")
            }
        }
        stage('pythonrun'){
            sh ("python3 abhi.py")
        }
    }
}